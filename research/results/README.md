# Evaluation results

Generated evaluation reports belong under this directory.

Results are intentionally ignored by Git except for this README. They should be reproduced from the committed evaluation scripts, dataset configuration, and local dataset artifacts instead of being treated as source files.

Large predictions, downloaded datasets, and model checkpoints must also remain uncommitted.
