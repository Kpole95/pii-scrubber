# Contributing

Run the full test suite and `python scripts/check_structure.py` before opening a change. Do not add generated environments, caches, model weights, private examples, generic utility modules, or runtime imports from `research`.

Every behavioural change requires focused tests and a concise public docstring when it affects an exposed API.
