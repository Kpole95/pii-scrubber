# tests

Automated checks for runtime contracts, integration behavior, and isolated units.

## Folders

- `contract/` — Shared detector contract tests.
- `integration/` — Public API and CLI integration tests.
- `unit/` — Focused unit tests.

## Files

- `__init__.py` — Test suite for the PII Scrubber project. Main defs/classes: No top-level defs or classes.

## Notes

Keep this guide short. Update it when files move, are added, or change responsibility.
